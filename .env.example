# Get your API key from https://aistudio.google.com/
# Free tier = 20 requests/day. Use multiple keys to get more: GEMINI_API_KEY=key1,key2,key3
GEMINI_API_KEY=your_api_key_here

# If you get "limit: 0" (region/quota), try: GEMINI_MODEL=gemini-2.5-flash or gemini-2.5-flash-lite
# GEMINI_MODEL=gemini-2.0-flash

# Quota-saver: 1 Gemini call per message (free tier ~20/day). Set to 0 for full extraction+compression
# GEMINI_SAVE_QUOTA=1

# Use VL-JEPA for embeddings (Mac/Apple Silicon only; requires mlx + vljepa)
# USE_VLJEPA=1
